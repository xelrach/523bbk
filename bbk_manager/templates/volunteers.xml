<?xml version="1.0"?>
<volunteers>
{% for vol in volunteers %}
<volunteer
		volunteer_id="{{ vol.id|e }}"
		first_name="{{ vol.first_name|e }}" 
		last_name="{{ vol.last_name|e }}"
		email="{{ vol.email|e }}"
		phone="{{ vol.phone|e }}"
		status="{{ vol.status|e }}" />
{% endfor %}
</volunteers>
